<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Get Data</title>
    <link rel="stylesheet" href="/stylesheets/show-data.css">
    <style>
      body {
        font-family: cursive;
      }
    </style>
  </head>
  <body>
    <header>
    <nav class="nav-bar">
        <ul>
          <li><a href="/">Create</a></li>
          <li><a href="show-data">Show Data</a></li>
          <li><a href="search">Search</a></li>
          <li><a href="update">Update</a></li>
          <li><a href="delete">Delete</a></li>
        </ul>
      </nav>
  </header>
    <h1 style="text-align: center">Showing All Data</h1>
    <% if(data.length > 0){ %> <% data.forEach(element => { %>
    <div class="data-display">
      <p><%= element.name %></p>
      <p><%= element.email %></p>
      <p><%= element.contact %></p>
    </div>
    <% }); %> 
    <% } %>

    <script></script>
  </body>
</html>
